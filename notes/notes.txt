					Admin Panel Setup

- Creating backend folder for the template and pasting all the asset and other files that are from the template
- Creating Admin Folder (we put everything admin related here)
- In Admin Folder we create admin_master.blade.php and index.blade.php
- In Admin Folder we create another folder - body
- Copying index template file and pasting it into admin_master.blade.php file
- {{ asset('') }} - redirects to public folder
- editing the css and js and image paths from template with asset
- creating files for static code - header, sidebar, footer
- creating file for main admin body part(index.blade.php) - dynamic part





					Admin User Logout
					
- adding href route in Log Out <a> tag
- Creating controller : php artisan make:controller AdminController
- Adding route to web.php
- Don't forget to use the Controller
- Creating Controller Method





					Custom Login Page
					
- Changing default login.blade.php contents with template contents
- updating css and js with asset
- Copying names, ids, routes from default login file inputs and pasting them into the input fields of the template so they work the same way
- Changing static names to dynamic(to the user that is logged in) - Auth::user()->name; Auth::user()->email





					Project Category
					
- Create migration for categories : php artisan make:migration create_categories_table --create=categories
- Create migration for subcategories : php artisan make:migration create_subcategories_table --create=subcategories
- Create fields and migrate
- Create folders for backend and frontend controllers
- Creating controller : php artisan make:controller Backend/CategoryController
- Adding routes to sidebar links
- Creating route in web.php
- Creating Method in Controller
- Creating backend and category folders in view
- Creating index page in category folder and returning it in the controller
- Creating index.blade.php page for the category in Admin 
- Add category method and routes
- Add category add page and category add methods and routes
- Display verification error in create.blade.php
- Adding toaster
- We have to create custom pagination, because the laravel pagination is not supported
- Creating file in main view file for custom pagination
- https://www.codecheef.org/article/laravel-7-custom-pagination-example-tutorial





					Project SubCategory

- Adding route in sidebar
- Creating Controller for subcategories : php artisan make:controller Backend/SubCategoryController
- Get all the data from categories table in create subcategory page(AddSubCategory method), so we can select which category we want to insert the subcategories in 
- Creating relation in Index Method in SubCategoryController





					Project District and SubDistrict

- Same as Category and SubCategory





					News Posts

- Adding Editor 
- Adding Image Intervention - http://image.intervention.io/
- Loading SubCategories and SubDistricts when we Select Category or discript(AJAX)
- Adding AJAX CDN and in admin_master : <meta name="csrf-token" content="{{ csrf_token() }}"> adding the csrf token
- Creating a web route for the AJAX url to get all the information
- $("#subcategory_id").empty(); We make it empty
- $.each(data,function(key,value){} - Gets the Key and the value of the data we get from the DB and we can use it(append it for example)
- Using Helper Class Laravel






